---
name: ci

on:
  push:
    branches:
      - main
    tags-ignore:
      - "*"
  pull_request:
    branches:
      - main

jobs:
  build:
    uses: moderneinc/gh-automation/.github/workflows/ci-gradle.yml@main
    secrets: inherit
